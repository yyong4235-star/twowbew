# 官网项目恢复节点

创建时间：2026-07-24
项目路径：`/Users/a1-6/Documents/公司网站`
当前分支：`codex/dj-electronics-site`
最新安全恢复提交：`860d3c6 style: update header logo asset`
最新已知功能基线：`26c1ada style: use logo artwork as hero background`

## 恢复用途

如果当前对话因为图片、上下文过大或浏览器状态异常而中断，新对话可以先读取本文件，再继续开发。

新对话建议开场直接说：

```text
请读取 /Users/a1-6/Documents/公司网站/docs/恢复节点.md，并从这个恢复节点继续公司官网开发。不要直接打开原始大图，先按文档里的图片安全流程处理。
```

## 当前网站状态

网站是纯静态项目，无构建步骤。

主要文件：

- `index.html`：页面入口、导航、语言切换容器。
- `script.js`：三语内容、hash 路由、页面渲染。
- `styles.css`：全部布局、响应式、动效样式。
- `assets/home-solutions-bg.png`：首页中段背景图。
- `assets/brand/logo.png`：当前头部 logo，来源于 `assets/incoming-brand/032954A5-4310-4380-A8E7-8C4F77531D08_1_201_a 2.jpeg`，已转换为 PNG，尺寸 `512 x 372`。
- `assets/brand/hero-logo-bg.png`：首页首屏 hero 背景图，来源于 `assets/incoming-brand/xw_20260724134527.png`。
- `assets/brand/about-main.jpg`：关于我们页大图，来源于 `assets/incoming-brand/7DD89FBE-2080-4B1C-8642-250D186AA2FF_1_201_a.jpeg`，已转换为 JPEG，尺寸 `1200 x 852`。
- `assets/home/home-01.jpg` 至 `assets/home/home-03.jpg`：首页中段 3 个核心业务卡片正式网页图。
- `assets/home-thumbnails/home-01.jpg` 至 `assets/home-thumbnails/home-03.jpg`：首页中段卡片图缩略图。
- `assets/products/product-01.jpg` 至 `assets/products/product-06.jpg`：产品页 6 个产品卡片正式网页图。
- `assets/product-thumbnails/product-01.jpg` 至 `assets/product-thumbnails/product-06.jpg`：产品图缩略图。
- `assets/manufacturing/manufacturing-01.jpg` 至 `assets/manufacturing/manufacturing-06.jpg`：制造能力页 6 个卡片正式网页图。
- `assets/manufacturing-thumbnails/manufacturing-01.jpg` 至 `assets/manufacturing-thumbnails/manufacturing-06.jpg`：制造能力图缩略图。
- `assets/oem/oem-01.jpg` 至 `assets/oem/oem-03.jpg`：OEM/ODM 页 3 个合作模式卡片正式网页图。
- `assets/oem-thumbnails/oem-01.jpg` 至 `assets/oem-thumbnails/oem-03.jpg`：OEM/ODM 图缩略图。

本地预览方式：

```bash
cd /Users/a1-6/Documents/公司网站
python3 -m http.server 4173
```

预览地址：

```text
http://localhost:4173/
```

也可以直接打开：

```text
file:///Users/a1-6/Documents/公司网站/index.html#home
```

## 已完成内容

公司官网首版已完成，默认越南文，支持英文和中文切换。

页面包括：

- 首页 `#home`
- 产品页 `#products`
- 制造能力 `#manufacturing`
- OEM/ODM `#oem`
- 关于我们 `#about`
- 联系方式 `#contact`

公司信息：

- 公司越南文全称：`CÔNG TY TNHH THIẾT BỊ ĐIỆN TỬ DJ`
- 英文全称：`DJ ELECTRONIC EQUIPMENT COMPANY LIMITED`
- 英文简写：`DJ ELECTRONICS CO., LTD`
- 成立时间：`2019`
- 地址：`Số nhà 083, phố Tuệ Tĩnh, tổ 10 Kim Tân, Phường Lào Cai, Tỉnh Lào Cai, Việt Nam`
- 电话显示：`+84 963 162 922`
- 电话链接：`tel:+84963162922`
- 邮箱：`congtytnhhthietbidientudj@gmail.com`

产品页 6 个产品卡片图片已按用户指定路径替换：

- `product-01.jpg`：新能源快充充电器。
- `product-02.jpg`：车载储能与设备专用充电器。
- `product-03.jpg`：锂电池 BMS 管理系统。
- `product-04.jpg`：大功率电池保护板。
- `product-05.jpg`：PCB 与 SMT 贴片。
- `product-06.jpg`：电路板半成品代工。

制造能力页 6 个卡片图片已按用户指定路径替换，并从产品页图片数组拆分为独立 `manufacturingImages`：

- `manufacturing-01.jpg`：技术研发。
- `manufacturing-02.jpg`：自动化 SMT。
- `manufacturing-03.jpg`：元器件焊接。
- `manufacturing-04.jpg`：电池 PACK。
- `manufacturing-05.jpg`：成品组装。
- `manufacturing-06.jpg`：出厂质量控制。

OEM/ODM 页 3 个合作模式卡片图片已按用户指定路径替换，并从产品页图片数组拆分为独立 `oemImages`：

- `oem-01.jpg`：OEM。
- `oem-02.jpg`：ODM。
- `oem-03.jpg`：专项定制开发。

关于我们页大图已替换为 `assets/brand/about-main.jpg`；头部 logo 已替换为新的 `assets/brand/logo.png`。

2026-07-24 修复产品页图片不显示：根因是 `Array.map(card)` 自动传入第三个参数数组，和 `card(item, index, images)` 的自定义图片数组参数冲突，导致产品图片路径渲染成 `[object Object]`。当前 `card()` 已校验第三个参数必须是字符串图片路径数组，否则回退到 `productImages`。

首页中段 3 个核心业务卡片图片已按用户指定路径替换，并从产品页图片数组拆分为独立 `homeImages`：

- `home-01.jpg`：新能源充电器。
- `home-02.jpg`：BMS 与电池保护板。
- `home-03.jpg`：PCB/SMT 与半成品。

隐私政策已接入官网：

- 新增独立路由 `#privacy`，默认越南文，支持英文和中文切换。
- 联系页增加隐私政策入口。
- 全站页脚增加隐私政策入口。
- 隐私政策内容面向 `BMS Protection Board Manager`，覆盖本地数据处理、蓝牙/位置/USB 权限、第三方 SDK、儿童隐私、用户权利和联系方式。

2026-07-24 全站 UI/UX 初步优化已完成：

- 未改动任何已上传图片文件，只调整网页代码。
- `styles.css` 已统一为更完整的工业科技视觉系统：深色科技背景、蓝绿紫点缀、统一 8px 圆角、统一阴影、卡片比例和 section 过渡。
- 产品、制造、OEM、首页卡片继续使用原有站内图片路径，卡片增加更稳定的高度、图片比例、hover 反馈和电流边框层级。
- 主 CTA 保留 StarBorder 风格，并统一按钮高度、按压反馈、焦点可访问性和移动端宽度。
- 首页上/中/下衔接重新整理：hero、home-solutions、home-contact-flow 之间改为连续深色到浅色过渡。
- 移动端导航优化：菜单图标有开合状态，点击导航自动收起，按 Esc 可关闭菜单。
- 修复一次优化中引入的 `data-route` 命名冲突：不要再给 `<body>` 设置 `data-route`，当前使用 `data-current-route`，导航选择器限定为 `.main-nav [data-route]`。

## 已加视觉效果

- 首页中段使用 `assets/home-solutions-bg.png` 作为科技背景。
- 首页上、中、下区块做了深色到浅色的过渡优化。
- 大卡片加了 CSS 电流边框效果：
  - 首页：3 个
  - 产品页：6 个
  - 制造能力下面：6 个
  - OEM：3 个
  - 关于我们：5 个
  - 联系方式：3 个
- 主要 CTA 按钮加了 StarBorder 风格动画。
- CTA 横幅加了 Prism 风格动态背景层。
- 曾在首页中段和 CTA 之间加入 Hyperspeed 风格过渡带，并提交为 `bc622e3`。
- 头部 logo 已从字母色块改为真实图片，logo 框尺寸为 `76 x 56`，图片使用 `object-fit: cover` 填满。
- 首页首屏 hero 背景已改为 logo 素材里的最大图片，并加深色遮罩保证标题可读。

## 当前需要注意的工作区状态

2026-07-24 全站 UI/UX 优化后，`git status` 显示 `script.js` 和 `styles.css` 有未提交改动，并存在 incoming 原始图片和 `.DS_Store` 未跟踪文件。不要把 incoming 原图或 `.DS_Store` 直接提交。

这些未提交改动目前包括：

- 保留之前删除 Hyperspeed 过渡 DOM/CSS 的状态。
- `styles.css` 已大幅重整：统一色彩 token、section 过渡、卡片比例、hover/active/focus 状态、移动端响应式和动效降级。
- `script.js` 增加 `data-current-route` 标记、移动端导航点击后自动收起、Esc 关闭菜单。
- 已修复 `document.body.dataset.route` 与导航 `[data-route]` 选择器冲突的问题，后续不要恢复这个错误写法。

不要盲目覆盖这些未提交改动。新对话应先运行：

```bash
git status --short --branch
git diff --stat
git diff -- script.js styles.css | sed -n '1,260p'
```

然后根据用户意图决定：

- 如果用户想保留当前优化效果，就基于当前未提交状态继续调整或提交。
- 如果用户想恢复 Hyperspeed 效果，需要谨慎参考提交 `bc622e3`，但不要覆盖本次 `data-current-route` 修复和全站样式优化。

## 最近提交记录

关键提交包括：

- `860d3c6 style: update header logo asset`
- `24c25d9 style: enlarge header logo fill`
- `11d03e2 style: replace header logo artwork`
- `26c1ada style: use logo artwork as hero background`
- `d90214c feat: add company logo to header`
- `264d657 chore: add incoming asset folders`
- `813278b docs: add recovery checkpoint`
- `bc622e3 style: brighten hyperspeed transition layer`
- `6a7dd42 style: add hyperspeed transition between home sections`
- `4d86157 style: add prism animation to CTA band`
- `85eb93a style: add star border effect to primary CTAs`
- `10d2daf style: add electric borders to major cards`
- `4d4fd32 style: smooth home section color transitions`
- `40931f2 style: deepen home solutions background treatment`
- `c24da19 style: add subtle background to home solutions section`
- `788fa24 fix: remove contact actions from CTA band`
- `395ada1 fix: populate contact cards with international phone`

## 图片处理安全流程

用户非常担心图片导致对话崩溃。必须严格按这个流程处理产品图片。

禁止事项：

- 不要直接把图片拖进聊天。
- 不要直接用 `view_image` 打开原始大图。
- 不要一次性读取很多图片。
- 不要在浏览器里直接加载原始大图。
- 不要把图片像素内容输出进对话。

推荐目录：

```text
/Users/a1-6/Documents/公司网站/assets/incoming-products/
```

安全流程：

1. 用户先只放 3 张测试图片。
   当前第一批测试图已放入 `assets/incoming-products/`，文件名为 `34387ef8-3d16-43e3-8dfc-2f72f5140817.png`、`0AE1F903-0F9C-4ED3-A0B0-625634B7F312_1_201_a.jpeg`、`Gemini_Generated_Image_xwbtg0xwbtg0xwbt.png`。
2. 只读取文件名、大小、格式、宽高，不读取图片内容到对话。
3. 生成低清缩略图到：

```text
/Users/a1-6/Documents/公司网站/assets/product-thumbnails/
```

4. 生成网页用压缩图到：

```text
/Users/a1-6/Documents/公司网站/assets/products/
```

5. 只查看一张低分辨率 contact sheet，不逐张打开原图。
6. 每处理一批就提交一次。
7. 第一批稳定后，再处理下一批。

建议的后续命令方向：

```bash
find assets/incoming-products -maxdepth 1 -type f -print
```

如需获取图片尺寸，优先使用系统工具或 Python/Pillow 读取 metadata，避免把图片内容注入对话。

## 下一步建议

## CMS 后台与部署状态

2026-07-24 已接入 `Decap CMS + Netlify` 方案：

- 后台入口：`/admin/`。
- 后台入口文件：`admin/index.html`。
- 后台配置文件：`admin/config.yml`。
- 公司信息和全站图片路径：`content/site.json`。
- 三语页面内容：`content/pages/vi.json`、`content/pages/en.json`、`content/pages/zh.json`。
- 三语隐私政策：`content/privacy/vi.json`、`content/privacy/en.json`、`content/privacy/zh.json`。
- 后台媒体目录：`assets/cms/`。
- 部署说明：`docs/部署说明.md`。
- 后台版前台通过 `fetch()` 读取 JSON，不建议继续使用 `file://` 预览，应使用本地服务器或线上域名。

推荐本地预览：

```bash
cd /Users/a1-6/Documents/公司网站
python3 -m http.server 4173
```

然后打开：

```text
http://localhost:4173/
http://localhost:4173/admin/
```

部署到 Netlify 后：

- 官网：`https://你的域名/`
- 后台：`https://你的域名/admin/`
- 隐私政策：`https://你的域名/#privacy`

注意：`admin/config.yml` 当前 `backend.branch` 为 `main`。如果 GitHub 默认分支不是 `main`，部署前需要改成实际分支名。

当前已完成提交包括：

- `b1cfb25 style: polish site visual system`
- `f89fe55 feat: extract site content to json`
- `b932fda feat: load site content from json`
- `c5af32a feat: add decap cms admin`

下一步如果用户继续提供产品图片或要求调整图片：

1. 继续坚持只读 metadata，不打开原图。
2. 转换为 `assets/products/` 下的压缩网页图。
3. 必要时同步生成 `assets/product-thumbnails/` 缩略图。
4. 浏览器验证桌面端、移动端、图片加载和无横向溢出。
5. 每完成一批图片替换就提交一次，并更新本恢复节点。

如果用户继续改视觉效果：

1. 先确认当前未提交的 Hyperspeed 删除是否是用户想要的结果。
2. 再继续改 CSS。
3. 每次视觉调整后用 Playwright 检查：桌面、移动端、无横向溢出、关键元素存在。

## 验证命令参考

```bash
git status --short --branch
curl -fsS http://127.0.0.1:4173/ >/dev/null && echo 'Preview server ok'
git diff --check
```

Playwright 使用 Codex bundled Node：

```bash
/Users/a1-6/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node
```

Playwright 包路径：

```text
/Users/a1-6/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright
```
